# 2026-09-04 — Sessão 07: o artigo, o primeiro PR da equipe e a governança que travava a si mesma

**Data:** 04/09/2026 · **Participantes:** Davi, Claude · **Duração:** —

## O que foi pedido

Quatro pedidos, na ordem em que vieram:

> "Vai ser em ABNT e sim precisa de abstract em inglês. Vamos desenhar a
> configuração do GitBook para o artigo."

> "Fecha o PR #4."

> "O merge está bloqueado para mim por causa da governança, o que devo fazer?"

> "Migra pro commit status, comenta com o Filipe e apaga as branches."

## O que foi respondido / feito

### O artigo ganhou lugar próprio

A pergunta que abriu a sessão não era sobre GitBook, era sobre pessoas: **oito
das quinze semanas do cronograma são prosa, não código.** Yasmin e Filipe vão
escrever muito mais do que vão programar. Exigir que passem por branch, commit
e Pull Request para redigir um parágrafo é barreira sem contrapartida.

Mas a `docs/` está fechada para escrita desde a sessão 06, com bom motivo: o
GitBook reformata o que toca, e o conteúdo técnico não sobrevive.

A saída foi separar os dois mundos:

| Diretório | Espaço no GitBook | Quem escreve | Por quê |
|---|---|---|---|
| `docs/` | *TCC_SDCAC Docs* | GitHub, só leitura no GitBook | Normalização destrói tabela, comentário e quebra de linha |
| `artigo/` | *Artigo* (a criar) | **GitBook, escrita liberada** | Prosa corrida sobrevive à normalização sem prejuízo |

A restrição do produto obrigou a esse desenho, não a preferência: **espaços do
GitBook não podem ter raízes aninhadas.** `artigo/` precisou nascer irmão de
`docs/`, e não dentro dele.

Criados: `artigo/README.md`, `SUMMARY.md`, `.gitbook.yaml` e os cinco capítulos
(`01-introducao` a `05-conclusao`) mais `referencias.md`. **Todos são esqueleto
com instruções — nenhuma linha de conteúdo acadêmico foi inventada.**

As diretrizes institucionais do PDF viraram [`docs/diretrizes.md`](../diretrizes.md),
com ABNT e abstract em inglês confirmados pelo Davi.

### O PR #4 e o PR #3

O #4 nasceu sozinho: era o workflow antigo do GitBook, que só conseguiu abrir
Pull Request depois de o Davi liberar a permissão do Actions na organização.
Fechado. O #3 foi mergeado e levou o `artigo/` para a `main`.

### O primeiro Pull Request de outra pessoa — e o que ele revelou

Às 14:17 o Filipe abriu o [PR #5](https://github.com/Codexrocks/TCC_SDCAC/pull/5),
preenchendo a linha dele em `uso-de-ia.md`. **O conteúdo estava certo.** Três
coisas em volta não estavam, e nenhuma era culpa dele:

1. **Branch `FilipeF4guiar-patch-1` e commit `Update uso-de-ia.md`.** O guia
   dizia *"nome da branch: `filipe/docs/assunto`"* como se o campo estivesse
   vazio esperando ser preenchido. Não está — o GitHub já o entrega com um nome
   que reprova, e é preciso apagar por cima. Isso não estava escrito em lugar
   nenhum.
2. **A seção Uso de IA ficou com os comentários do modelo.** As linhas entre
   `<!--` e `-->` são instrução, não resposta, e somem quando o PR é exibido.
3. **O check `governanca` aprovou mesmo assim** — e esse é o achado grave.

O terceiro item virou investigação. O GitHub devolve o corpo do Pull Request com
**CRLF**. Depois de remover os comentários, o campo vazio ficava assim:

```
- **IA usada:**  \r
```

E `\r` não é removido por `.strip(" *_`-")`. O campo em branco parecia
preenchido. **A verificação de declaração de IA estava aprovando template
intacto desde que foi escrita** — só ninguém tinha aberto um PR pela interface
do GitHub para descobrir.

Os testes não pegaram porque as strings de teste foram escritas à mão, com `\n`
puro. Corrigido em `sem_comentarios`, com cinco casos novos no formato real, e
verificado que eles **falham** se a normalização for removida.

### O gancho e o guia

Dois tropeços com a mesma raiz — a ferramenta mais rígida que a regra que ela
imita:

- O gancho `commit-msg` barrava commit de merge pedindo `Assistido-por:`, sendo
  que o `validar.py` usa `--no-merges` e ignora esses commits desde sempre.
  Agora ele sai antes quando existe `MERGE_HEAD`.
- O [Guia do GitHub](../guia-github.md) passou a mostrar lado a lado **o que vem
  preenchido** e **o que escrever por cima**.

### A governança travava a si mesma

O Davi não conseguiu mergear o PR #3 mesmo com tudo aprovado. A causa não era
regra do projeto, era mecânica do GitHub:

**Check runs de mesmo nome se acumulam no mesmo commit.** O workflow da
governança roda de novo a cada revisão enviada, sempre no mesmo commit, e o
caminho normal é reprovar primeiro (sem aprovação) e passar depois (com as
aprovações). A reprovação antiga continuava contando, e o rollup ficava
`FAILURE` com tudo verde na tela.

A correção estrutural: o resultado passou a ser publicado como **commit status**,
que a documentação do GitHub define como

> success if the latest status for all contexts is success

— vale o mais recente por contexto, e o histórico não atrapalha.

O job foi renomeado de `governanca` para **`verificar`**, e o status publicado
chama-se **`governanca`**. Os dois precisam de nomes diferentes: quando um check
e um status compartilham o nome, o GitHub exige que **ambos** passem, e o check
run voltaria a travar tudo.

Verificado no commit `8291b05`: o rollup passou a trazer `verificar` (check run,
não obrigatório), `validar` (check run, obrigatório) e `governanca`
(**StatusContext**, obrigatório), publicado por `github-actions[bot]`. Nenhum
check run fantasma chamado `governanca` sobrou esperando.

#### A prova apareceu sozinha, no commit seguinte

No commit `e32a05d` o workflow rodou **duas vezes** no mesmo commit — uma pelo
push, outra pela edição do corpo do PR. O rollup ficou assim:

| Nome | Espécie | Quantas vezes aparece |
|---|---|---|
| `verificar` | CheckRun | **2** — os dois `FAILURE`, somando |
| `validar` | CheckRun | 1 |
| `governanca` | **StatusContext** | **1** |

É o defeito antigo e a correção lado a lado, no mesmo commit. O check run
duplicou e acumulou; o commit status foi substituído e ficou um só. Se a
governança ainda fosse check run, esse PR já teria **dois** `governanca`
vermelhos empilhados — e o segundo continuaria contando mesmo depois de as
aprovações chegarem.

O `FAILURE` do status está certo: o PR toca `scripts/` e `.github/`, exige duas
aprovações e tem zero.

#### E o caso extremo apareceu no PR #7

Minutos depois, o Filipe abriu o [PR #7](https://github.com/Codexrocks/TCC_SDCAC/pull/7)
a partir da `main` — ou seja, **com o workflow antigo**, ainda em check run. O
rollup dele:

```
governanca  CheckRun  FAILURE   ← oito vezes
validar     CheckRun  FAILURE
```

**Oito** check runs `governanca` empilhados, um por edição do corpo do PR. Com a
regra antiga, mesmo que a nona execução passasse, as oito anteriores continuariam
contando e o PR ficaria travado para sempre. É o defeito em tamanho real, num PR
que não é meu.

De quebra, confirma que a transição é segura nas duas direções: branch com o
workflow antigo publica o contexto `governanca` como check run, branch com o novo
publica como status, e **o ruleset aceita os dois** — o nome exigido não mudou.
Branch velha não trava esperando um status que ninguém publica.

### Limpeza

Cinco branches apagadas. Restaram quatro: `main`, a do PR aberto e as duas do
GitBook.

Descoberta pelo caminho: `git merge-base --is-ancestor` **nunca** confirma que
uma branch foi mergeada neste repositório, porque o merge é sempre *squash* e o
commit original não vira ancestral de nada. A conferência passou a usar o estado
do Pull Request.

## Decisões tomadas

| Decisão | Motivo |
|---|---|
| Artigo em espaço próprio, com escrita liberada no GitBook | Oito das quinze semanas são prosa; obrigar quem escreve a usar Git é barreira sem contrapartida |
| `artigo/` irmão de `docs/`, não dentro | Espaços do GitBook não aceitam raízes aninhadas — restrição do produto, não escolha |
| `docs/` continua só leitura | A normalização do GitBook destrói tabela, comentário e quebra de linha |
| Capítulos entram como esqueleto, sem conteúdo | Regra 5: não inventar conteúdo acadêmico. O texto é de quem assina |
| Governança vira commit status, não check run | Check run acumula resultados no mesmo commit; status vale pelo mais recente |
| Job renomeado para `verificar` | Nome igual entre check e status faz o GitHub exigir os dois |
| `verificar` **não** entra nos checks obrigatórios | Ele acumula falhas por natureza; quem decide é o status |
| Não recriar o PR do Filipe por conta própria | A autoria é dele, e a declaração de IA precisa das respostas dele |
| Conferir merge pelo estado do PR, não por ancestralidade | *Squash* apaga a linhagem do commit original |

## Alterações no repositório

| Arquivo | O que mudou |
|---|---|
| `artigo/` (9 arquivos) | **Novo.** Estrutura do artigo como segundo espaço do GitBook, com escrita liberada |
| `docs/diretrizes.md` | **Novo.** Diretrizes institucionais do TCC — ABNT e abstract em inglês |
| `.github/workflows/governanca.yml` | Job renomeado para `verificar`; publica o commit status `governanca` |
| `scripts/governanca.py` | `sem_comentarios` normaliza CRLF; `strip` cobre tabulação e retorno de carro |
| `tests/test_governanca.py` | Cinco casos com o formato real do GitHub, verificados contra mutação |
| `.githooks/commit-msg` | Sai antes em commit de merge, espelhando o `--no-merges` do validador |
| `docs/guia-github.md` | Mostra o que o GitHub preenche sozinho e o que escrever por cima |
| `docs/configuracao.md` | Nova seção: por que `validar` é check run e `governanca` é commit status |
| `docs/desempenho-das-ias.md` | Erros novos; balanço atualizado para 25 |
| **28 arquivos + `.github/CODEOWNERS`** | **"Felipe" → "Filipe"** em todo o repositório. `scripts/validar.py` aceita as duas grafias durante a transição |

## Erros da IA e correções

| O que a IA errou | Tipo | Como apareceu | Como foi corrigido | Quem pegou |
|---|---|---|---|---|
| A checagem de declaração de IA aprovava template em branco quando o corpo vinha com CRLF | processo | O PR #5, o primeiro aberto por outra pessoa, passou com os quatro campos vazios | `sem_comentarios` normaliza o fim de linha; cinco testes no formato real | **a pessoa** — o Davi mandou investigar por que o PR passou |
| Escreveu os testes com dado inventado à mão, e não com o dado real do GitHub | defeito latente | Os testes passavam e a função estava errada | Casos novos copiados do corpo real de um PR | a própria IA, já investigando o erro acima |
| Gancho `commit-msg` barrava commit de merge | conteúdo | `git merge origin/main` de rotina foi interrompido pedindo `Assistido-por:` | Sai antes quando existe `MERGE_HEAD` | a própria IA, ao tropeçar nele |
| Guia não avisava que o GitHub entrega branch e commit já preenchidos | processo | Derrubou o primeiro PR aberto por alguém da equipe | Guia mostra "o que vem" e "o que você escreve" lado a lado | **a pessoa** — apareceu no PR do Filipe |
| Quebrou o heredoc do shell com aspas — pela terceira vez no mesmo dia | sintaxe | `unexpected EOF while looking for matching '` | Passou a escrever pela ferramenta de escrita | a própria IA |
| Comentou o PR #5 com uma correção longa, e o pedido concreto ficou enterrado nela | processo | O Filipe fechou o PR e abriu o #7 **sem as quatro respostas** que o comentário pedia | Nada a desfazer. O erro é de proporção | **a pessoa** — pelo que fez em seguida |
| Concluiu que o Filipe tinha desistido, e registrou a conclusão como fato | conteúdo | Escreveu no `desempenho-das-ias.md` que o comentário "levou o autor a fechar o próprio PR". Onze minutos depois veio o PR #7 | Linha reescrita. Ele fechou para **recomeçar** | a própria IA, ao ver o #7 aparecer |
| Assunto de commit com 73 caracteres, um a mais que o limite da própria regra | sintaxe | `aviso  commit e32a05d4: assunto com 73 caracteres` | Nenhuma. Reescrever o histórico para esconder um aviso do próprio verificador seria pior que o aviso | **o check** |
| **Escreveu "Felipe" no lugar de "Filipe" desde a sessão 01** | conteúdo | Nenhum sintoma em sete sessões e 28 arquivos. O login `@FilipeF4guiar` estava certo o tempo todo, às vezes na mesma linha | Corrigido em todo o repositório; `felipe` virou alias transitório na lista de autores | **a pessoa** — o Davi |

> **O comentário estava certo e mesmo assim não funcionou.** Ele explicava a
> causa, assumia a culpa do guia e admitia o defeito do check — tudo verdadeiro.
> Mas tinha três seções, uma tabela, e um pedido de quatro linhas enterrado no
> meio. O pedido não foi atendido. **Completude não é comunicação:** quanto mais
> correto o texto fica, mais fácil é a única frase acionável desaparecer nele.
>
> E o erro seguinte é pior, porque contaminou o próprio registro. Do fechamento
> em 45 segundos a IA deduziu desistência e **escreveu a dedução como
> observação**, num documento que existe para separar as duas. O PR #7
> desmentiu em onze minutos. Ficou nesta tabela de propósito: é a evidência mais
> limpa até agora de que a IA não distingue o que viu do que supôs.

> **O erro do CRLF é o retrato do limite dos testes escritos pela mesma IA que
> escreve o código.** Ela inventa a entrada, e inventa uma entrada que o código
> dela já trata. É o mesmo padrão do `command -v python3` na sessão anterior:
> só o dado real reprova.

## Pendências

- [ ] **Remover `"felipe"` da lista `AUTORES`** em `scripts/validar.py` e do
      teste correspondente, assim que esta correção estiver na `main`. Ela está
      lá só porque os workflows validam o PR com o `validar.py` da base: enquanto
      a `main` tiver a lista antiga, uma branch `filipe/...` seria reprovada
      sem que ninguém entendesse por quê · **Davi** · no PR seguinte ao merge
- [ ] **Decidir se a regra de nome de branch vale para quem edita pelo
      navegador.** O Filipe esbarrou nela duas vezes seguidas, nos PRs #5 e #7,
      e não por descuido: o GitHub entrega o campo preenchido com
      `<login>-patch-N` e é preciso apagar por cima. Ou o guia resolve, ou a
      regra abre exceção para mudança só em `docs/`. **Mudança de regra é
      decisão do Davi, não da IA** · **Davi** · antes de 10/09
- [ ] **PR #7 está travado** com oito `governanca` vermelhos e o `validar`
      reprovando nome de branch e mensagem de commit. Depende da decisão acima ·
      **Davi** · agora
- [ ] Preencher a seção **Uso de IA** do PR #7 com os quatro campos. No #7 ela
      veio como `<-- ... -->`, que não é comentário HTML válido — falta o `!` —
      e os quatro campos não estão lá · **Filipe** · antes da próxima sessão
- [ ] Criar o espaço **Artigo** no GitBook apontando para `./artigo`, com
      escrita liberada · **Davi** · antes de 10/09
- [ ] Preencher a linha da tabela em [`uso-de-ia.md`](../uso-de-ia.md) ·
      **Yasmin** · antes da próxima sessão
- [ ] Rodar `git config core.hooksPath .githooks` no clone de cada um ·
      **Yasmin e Filipe** · antes do primeiro commit
- [ ] **Introdução do artigo** — primeira entrega com prazo · **equipe** ·
      10/09
- [ ] Confirmar que o relatório semanal roda na segunda; se falhar, disparar na
      mão pela aba Actions · **Davi** · 07/09
- [ ] Combinar o que "aprovar" significa: ler o diff, não concordar com a ideia ·
      **Davi** · próxima reunião
